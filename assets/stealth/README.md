`profile.tar.xz` contains the `profile.selenium` profile and the `profile.ini` file setting it as default.

The profile is set as follow :
- Browser Language : en-us
- Security : Safer
- Extensions :
  - Tampermonkey : webdriver.js

